const post = require(".");

const read = (params => {
  // return new Promise((resolve, reject) => {
  //   post.find(params.query)
  // })
});

const update = (params => {

});

const destroy = (params => {

});

module.exports = {
  read,
  update,
  destroy
}